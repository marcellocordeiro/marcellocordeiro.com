---
import { DesktopHeader } from "./DesktopHeader";
import { MobileHeader } from "./MobileHeader";

interface Props {
  pathname: string;
}

const { pathname } = Astro.props;
---

<DesktopHeader className="not-md:hidden" {pathname} />
<MobileHeader className="md:hidden" {pathname} client:load />
